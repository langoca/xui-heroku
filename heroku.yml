build:
   docker:
      web: Dockerfile
run:
   web: mkdir x-ui && cd x-ui
   web: docker run -itd --network=host -v $PWD/db/:/etc/x-ui/ -v $PWD/cert/:/root/cert/ --name x-ui --restart=unless-stopped enwaiax/x-ui:latest
